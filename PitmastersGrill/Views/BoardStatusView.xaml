<UserControl x:Class="PitmastersGrill.Views.BoardStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="72"
             d:DesignWidth="760">
    <Border x:Name="BoardStatusFooter"
            Margin="0,6,0,0"
            Padding="8,5"
            Background="{DynamicResource SurfaceAltBrush}"
            BorderBrush="{DynamicResource PanelBorderBrush}"
            BorderThickness="1"
            AutomationProperties.AutomationId="BoardStatusFooter">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="BoardPopulationStatusText"
                       Grid.Row="0"
                       Grid.Column="0"
                       Foreground="{DynamicResource MutedTextBrush}"
                       Text="Board population idle"
                       TextWrapping="NoWrap"
                       TextTrimming="CharacterEllipsis"
                       VerticalAlignment="Center"
                       Margin="0,0,12,0"
                       AutomationProperties.AutomationId="BoardPopulationStatusText"/>

            <TextBlock x:Name="LastRefreshedText"
                       Grid.Row="0"
                       Grid.Column="1"
                       Foreground="{DynamicResource MutedTextBrush}"
                       Text="Last Refreshed: --"
                       TextWrapping="NoWrap"
                       TextTrimming="CharacterEllipsis"
                       VerticalAlignment="Center"
                       AutomationProperties.AutomationId="LastRefreshedText"/>

            <TextBlock x:Name="BoardSummaryText"
                       Grid.Row="1"
                       Grid.Column="0"
                       Grid.ColumnSpan="2"
                       Foreground="{DynamicResource BodyTextBrush}"
                       Text="Visible 0 | Watched 0"
                       Margin="0,5,0,0"
                       TextWrapping="Wrap"
                       TextTrimming="CharacterEllipsis"
                       VerticalAlignment="Center"
                       AutomationProperties.AutomationId="BoardSummaryText"/>
        </Grid>
    </Border>
</UserControl>
